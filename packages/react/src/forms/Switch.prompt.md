**Switch** — instant on/off toggle for settings (notifications, preferences). Use for immediate effect; use Checkbox for form submission choices.

```jsx
<Switch label="Email notifications" defaultChecked />
```
