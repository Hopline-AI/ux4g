**Radio** — single-select within a group. Give every option in a group the same `name`.

```jsx
<Radio name="gender" value="f" label="Female" defaultChecked />
<Radio name="gender" value="m" label="Male" />
```
