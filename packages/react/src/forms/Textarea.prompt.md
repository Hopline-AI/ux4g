**Textarea** — multi-line text input for grievance descriptions, feedback and comments.

```jsx
<Textarea label="Describe your grievance" rows={5} required
          helperText="Be as specific as possible." />
```

- **state**: `default` · `error` · `success` · `rows`, `helperText`, `required`.
