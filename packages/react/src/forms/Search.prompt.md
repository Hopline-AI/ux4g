**Search** — a rounded search field with a leading magnifier and a clear (×) button; fires `onSearch` on Enter.

```jsx
<Search placeholder="Search 2,400+ services" onSearch={runSearch} size="large" />
```

- Controlled (`value`/`onChange`) or uncontrolled. `onSearch` fires on Enter.
