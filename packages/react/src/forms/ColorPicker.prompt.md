**ColorPicker** — choose a brand/theme colour (mirrors UX4G's themeable primary/secondary). Curated swatches plus an optional native custom picker.

```jsx
<ColorPicker label="Primary colour" defaultValue="#613AF5" onChange={setPrimary} />
```

- `swatches` overrides the default palette. `allowCustom={false}` hides the picker.
