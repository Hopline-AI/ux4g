**Breadcrumb** — shows where the user is in the site hierarchy. The last item is the current page (non-link).

```jsx
<Breadcrumb items={[
  { label: "Home", href: "/" },
  { label: "Services", href: "/services" },
  { label: "Apply for Certificate" },
]} />
```
