**EmptyState** — reassuring placeholder when there's nothing to show yet (no applications, no search results).

```jsx
<EmptyState
  icon={<InboxIcon />}
  title="No applications yet"
  description="When you apply for a service, it'll appear here."
  action={<Button>Browse services</Button>}
/>
```
