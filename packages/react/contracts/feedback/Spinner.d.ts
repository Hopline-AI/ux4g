import * as React from "react";

/** UX4G Spinner — small indeterminate loading indicator. */
export interface SpinnerProps {
  size?: number;
  color?: string;
  label?: string;
}

export declare function Spinner(props: SpinnerProps): JSX.Element;
export default Spinner;
